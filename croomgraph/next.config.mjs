/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false, 
    output: "standalone",
    // images: {
    //     unoptimized: true, // If you're serving images without Next.js optimization
    //   },

};

export default nextConfig;
